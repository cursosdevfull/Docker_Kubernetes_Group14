# Utils

```
docker exec -i <nombre o id del contenedor> <nombre del programa, librería o paquete que quieran usar>
docker exec -i -t <nombre o id del contenedor> <nombre del programa, librería o paquete que quieran usar>
docker exec -t -i <nombre o id del contenedor> <nombre del programa, librería o paquete que quieran usar>
docker exec -it <nombre o id del contenedor> <nombre del programa, librería o paquete que quieran usar>
docker exec -ti <nombre o id del contenedor> <nombre del programa, librería o paquete que quieran usar>
```
